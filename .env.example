SECRET_KEY=replace-with-a-long-random-string
JWT_SECRET=replace-with-a-different-long-random-string
DB_PASSWORD=change-me-to-a-strong-password
# Optional SMTP for password-reset/contact email (app runs fine without it):
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# EMAIL_USER=
# EMAIL_PASSWORD=

# Custom domain for HTTPS via Caddy (e.g. votesmartt.duckdns.org)
DOMAIN=
